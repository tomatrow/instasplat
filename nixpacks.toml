[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmd = "echo 'starting install phase' && pnpm install --frozen-lockfile && echo 'finished install phase'"

[phases.build]
cmd = "echo 'starting build phase' && cd packages/app && echo 'in packages/app' && pnpm run build && echo 'finished build phase'"

[start]
cmd = "echo 'start' && cd packages/app && echo 'in packages/app' && node build/index.js"

[variables]
NODE_ENV = "production"
